{erl_opts, [debug_info,
            {parse_transform, lager_transform},
            {lager_truncation_size, 1024}]}.
{deps, [
        lager,
        {mnesia_rocksdb, {pkg, tivan_rocksdb}},
        {uuid, {pkg, uuid_erl}}
       ]}.

{plugins, [rebar3_hex]}.

{shell, [
    {apps, [tivan]}
]}.
